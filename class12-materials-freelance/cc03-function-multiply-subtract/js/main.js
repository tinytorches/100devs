// Coding Challenge: Create a function that takes in 3 numbers. The function should return the first number multiplied by the second and then subtract the third. Console.log() the resulting value

// Create a function that takes 3 numbers(parameters)
function threeNumbersMultipliedAndSubtracted(num1, num2, num3) {
// Return the first number, multiplied by the second, and subtracted by the third
    const resultingValue = num1 * num2 - num3
// console.log() the resulting value
    console.log(resultingValue)
}
// enter function with arguements into the browser console
// i.e. "threeNumbersMultipliedAndSubtracted(2, 3, 4)"